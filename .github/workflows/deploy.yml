name: Deploy to App Engine
on:
  push:
    branches:
      - EPO-5131
jobs:
  deploy_this_noise:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Deploy to App Engine
        uses: google-github-actions/deploy-appengine@main
        env:
          I_DB_SERVER: ${{ secrets.CLOUD_SQL_PRIVATE_IP }}
          I_DB_USER: ${{ secrets.CLOUD_SQL_DB_USER }}
          I_DB_PASSWORD: ${{ secrets.CLOUD_SQL_DB_PASS }}
          I_DB_DSN: ${{ secrets.CLOUD_SQL_DB_HOST }}
          I_ERIC: rosas
        with:
          deliverables: test/app.yaml
          project_id: skyviewer
          credentials: ${{ secrets.GAE_SA }}
